{
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "backend/Docker",
        "buildContext": "backend"
    },
    "deploy": {
        "numReplicas": 1,
        "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port 8000"
    }
}